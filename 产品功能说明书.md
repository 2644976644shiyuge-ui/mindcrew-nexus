# MindCrew 产品功能说明书

**企业级 Agentic RAG 智能知识中枢**

适用版本：商用授权版
文档性质：面向采购方的产品功能说明
更新日期：2026-06-18

---

## 一、产品概述

MindCrew 是一套面向企业的智能知识问答与知识资产管理系统。它把分散在 PDF、Word、PPT、Excel、音视频、聊天记录中的企业文档，转化为可以直接对话、精准溯源、可被外部系统调用的智能知识资产。

与传统「向量检索 + 拼接回答」的 RAG 系统不同，MindCrew 的检索引擎由大语言模型实时驱动：模型根据每个问题的特征，动态选择检索工具、调整召回策略，并对最终答案进行自检纠错。系统在此之上提供了完整的多智能体调研、培训教练、实时语音通话、对外开放 API、组织权限、计费对账、安全合规等企业落地所需的全套能力。

系统采用全栈开源组件构建，零厂商锁定，完全支持私有化部署。所有核心参数支持数据库动态配置，无需改代码、无需重启。

### 产品定位

| 维度 | 说明 |
|:-----|:-----|
| 目标客户 | 有内部知识沉淀需求的中大型企业、政企单位、专业服务机构、培训部门 |
| 核心场景 | 内部知识问答、制度/规范查询、客服支持、新人培训、专题调研、对外知识服务 |
| 交付方式 | 私有化部署（物理机 / 内网 / 专有云），数据全程留在客户侧 |
| 接入方式 | Web 端、语音通话、钉钉机器人、对外 HTTP API、MCP 标准协议 |

### 产品核心价值

1. **答案可追溯**：每条回答都附带精确来源（文档名、章节、页码、网页 URL）和完整推理链，不是黑箱。
2. **检索质量高**：七步精炼检索链路（改写、多路召回、融合、重排、压缩、自检），逐级提升答案准确度。
3. **能力可开放**：通过 MCP 标准协议和 HTTP API，把企业知识库变成可被任意 AI 客户端、自动化流程调用的服务。
4. **落地能力全**：不止于问答，覆盖培训、调研、语音、组织权限、计费、合规审计等企业真实运营需求。
5. **运营成本可控**：内置用量统计、成本归集、与云厂商真实账单对账，每一笔 AI 调用成本可追踪到人。

---

## 二、系统架构概览

```
            Web 前端 / 语音通话 / 钉钉机器人 / 外部 API / MCP 客户端
                                  │
                                  ▼
        ┌─────────────────────────────────────────────────┐
        │              MindCrew 应用服务（Spring Boot）        │
        │                                                   │
        │  Agentic RAG 引擎  ·  多智能体 Crew  ·  教练引擎     │
        │  语音通话网关  ·  MCP Server  ·  开放 API 网关       │
        │  权限/ACL  ·  计费/对账  ·  审计/脱敏               │
        └─────────────────────────────────────────────────┘
              │           │            │           │
              ▼           ▼            ▼           ▼
          MySQL 8     Milvus 2.x    Redis 7     MinIO
        （元数据）    （向量检索）  （缓存/记忆）（原始文档）
                                  │
                                  ▼
              大模型 / 嵌入模型 / 重排模型 / 联网搜索
        （兼容 OpenAI 协议，支持多厂商，可热切换）
```

系统所有外部依赖均为标准开源组件或可替换的兼容服务，不绑定任何单一云厂商。

---

## 三、核心功能模块

### 3.1 智能问答（Agentic RAG）

智能问答是系统的核心入口，提供对话式的知识检索体验。

**问答体验**

- 字符级流式输出，回答边生成边展示，无需等待。
- 可指定知识库范围提问，避免跨库信息干扰。
- 追问感知：自动结合对话历史与用户长期记忆，理解「继续说刚才那个」「它的下一步呢」等上下文。
- 每条回答自动附带精确来源标注：文档名、章节、页码、网页链接。
- 支持对回答点赞 / 点踩并填写纠正意见，沉淀为系统持续优化的数据。

**智能检索引擎（七步链路）**

每个问题进入系统后，由大模型驱动经过以下处理，逐级提升答案质量：

1. **查询改写**：将口语化问题改写为检索优化表达（过短查询自动跳过，改写失败自动回退原问题）。
2. **大模型选工具**：模型分析问题特征，动态决定调用哪些检索工具——语义向量检索、关键词精确检索、联网搜索、长期记忆，可组合可追加。
3. **多路融合（RRF）**：将向量、关键词、联网多路结果用倒数秩融合算法统一排序、去重。
4. **精排重排**：用 Cross-Encoder 重排模型对候选与问题计算精细相关度，取最相关的若干片段。
5. **上下文压缩**：去重、截断、控制总量，在保证信息完整的前提下降低成本。
6. **Prompt 组装与安全护栏**：注入带来源标注的参考资料、用户画像、长期记忆与近期对话；执行紧急关键词检测与置信度评估，来源不足时如实告知、不编造。
7. **流式输出与自检纠错**：回答生成后由模型自检事实一致性、完整性、来源匹配度，置信度不足时自动重检，最多两轮纠错。

**不同问题走不同路径**：问「最新政策变化」会触发联网搜索，问「第三条第二款的规定」会优先走关键词精确检索，问「继续说刚才那个」会先回忆上下文再检索。系统始终为每个问题选择最合适的检索组合。

**生产级容错**：每个环节都有降级方案。改写失败回退原问题、选工具失败回退规则路由、重排模型不可用降级为关键词评分、来源不足时明确告知用户。任一环节异常都不会导致整体不可用。

### 3.2 知识库管理（两层架构）

系统采用清晰的「知识库 — 文档 — 切片」三级模型，权限语义统一、不混乱。

```
知识库（权限边界 + 对外接口）── 文档（内容载体）── 切片（向量化单元）
```

**知识库（Collection）**——权限单元与对外接口单元

- 创建、命名、自定义图标、主题色、描述。
- 三档可见性：`公开`（所有人可见）/ `受限`（按授权访问）/ `私有`（仅创建者）。
- 库级访问控制（ACL）：按职位或部门授权，分 读 / 写 / 管理 三级权限，部门级支持上级部门继承。
- 库级 API Key：可颁发给第三方系统调用，按月配额计费，支持随时吊销。
- 文档数、切片总数等统计字段异步自动刷新。

**文档（Document）**——纯内容载体

- 归属某个知识库，权限完全继承所属库，无需单独配置，一处管全部。
- 支持上传、解析、切片、向量化、删除、移入库、重新归类。
- 未归库的散文档仅创建者自己可见。

**权限隔离（商用合规）**

文档「能否被检索」严格等于其所属知识库「能否被访问」。每次问答前，系统一次性取出当前用户可访问的全部知识库及其文档集合，越权检索在物理层面不可能发生，且不产生 N+1 查询。

### 3.3 文档处理

**全格式支持**

PDF、DOCX、PPT、XLS、WPS、HTML、纯文本、图片、音频、视频、微信聊天记录等主流格式全覆盖。

**上传体验**

- 全局上传队列 + 后台悬浮 Dock，上传过程中可自由切换菜单不中断。
- 并发上传（默认 3 路并行）、文件夹整体拖拽、前端大小/格式预校验、失败重试。
- 大文件分片并行上传（10MB 以上 8 线程并行），相比单线程提速 3-5 倍。
- 推荐流程：先建知识库，进库后「上传到此库」，文档自动归类继承权限；也兼容先散传再批量移入库。

**智能处理流水线**

- 自动提取元数据：章节、页码、内容类型、音视频时间戳。
- 视频：原生多模态理解，画面与字幕一次性产出可检索内容。
- 音频：逐句时间戳转写。
- 文档：段落 + 句子 + 滑动窗口智能切分。
- 切片写入元数据库，向量写入向量库，双索引保障语义与关键词双路检索。
- 处理状态全程可追踪（上传中 → 处理中 → 就绪 / 失败），失败文档支持一键重新处理。
- 并发向量化：多线程并行嵌入，百级切片处理从约 30 秒降至 4-6 秒。

### 3.4 多智能体调研（Agent 调研 / Crew）

面向复杂、开放性的专题问题，系统提供多智能体协作调研能力。提交一个调研任务后，系统自动编排多个专职 Agent 协同完成：

- **规划 Agent**：拆解任务，制定调研计划。
- **研究 Agent**：多路并行检索与取证。
- **写作 Agent**：综合材料产出结构化报告。
- **评审 Agent**：审查质量，不通过则退回修订，再次评审。

**全过程可视化**

- 实时事件流（SSE）推送每一步进展。
- 推理链回放：完整重现每个 Agent 的思考 → 行动 → 观察过程。
- Agent 通信图谱：可视化展示多智能体之间的协作与信息流转。
- 任务历史列表，随时回看过往调研。

### 3.5 教练模式（培训陪练）

面向新人培训与知识考核场景，把企业知识库变成自动出题的智能教练。

- 选定知识库范围与难度，系统从知识库原文自动生成成套练习题。
- **反幻觉保障**：每道题强制绑定一段原文，题目引用必须是原文真子串，校验不通过的题目直接丢弃，杜绝编造。
- 逐题作答、自动评分、即时反馈。
- 个人学习统计：练习次数、得分、进步曲线。
- 团队统计：主管与管理员可查看团队整体学习情况。
- 教练规则可由管理员配置，并绑定到具体知识库。

### 3.6 实时语音通话

提供与知识库的实时语音对话能力，适用于免手操作、客服、陪练等场景。

- 持续监听 + 语音活动检测（VAD），AI 说话时用户可随时打断，自然如真人对话。
- 通话中可实时切换知识库范围与联网开关。
- 支持键入文本消息（跳过语音直接对话）。
- 多音色可选，支持音色复刻。
- 降噪、打断灵敏度等运行参数由管理员在配置中心可视化调整，对新发起的通话即时生效。

### 3.7 技能包

技能包是「提问时可一键切换的场景化能力」。每个技能包封装了角色设定、回答风格、输出格式，并可绑定特定知识库范围。

- 例如「合同审查助手」「客服话术」「技术方案撰写」等，用户提问时选择对应技能包即可获得专业化的回答风格与范围。
- 管理员维护技能包（名称、图标、技能指令、绑定知识库、启用状态、排序）。
- 普通用户在提问界面直接选用。

### 3.8 Soul 人格

人格定义「AI 用什么风格、什么底线、什么口吻回答用户」，作为系统级提示词注入面向最终用户的回答阶段。

- 可创建多个人格（如「严谨研究员」「亲和客服」），设定完整提示词、温度、推荐模型。
- 反讨好开关：可在提示词末尾追加「反讨好」规则段，避免一味迎合、保持客观。
- 支持设为全局默认人格，支持预览提示词组装效果。
- 人格仅作用于面向用户的回答风格，不影响内部检索、评测等流程。

### 3.9 跨会话长期记忆

系统能自动记住用户偏好，实现真正的个性化知识服务。

- 用户表达「我是 Java 开发者」「我对花生过敏」等偏好时，系统自动提取并写入记忆（默认保留 30 天）。
- 下次对话自动召回，回答更贴合用户身份与需求。
- 记忆读写同样作为标准工具暴露，可被内部 Agent 与外部客户端调用。

---

## 四、开放与集成能力

### 4.1 MCP 标准协议开放

系统通过 MCP（Model Context Protocol）标准协议，把知识库的全部检索能力对外暴露为标准工具。任何支持 MCP 的客户端（Claude Desktop、Cursor、自研 Agent 等）都可直接发现并调用：

- `doc_search`——语义向量检索
- `keyword_search`——关键词精确检索
- `web_search`——联网实时搜索
- `recall_memory`——读取长期记忆
- `store_memory`——写入用户偏好

**一套工具实现，两种接入方式**：内部 Agent 与外部 MCP 客户端共享同一组工具实现。企业知识库由此从孤岛变为「知识即服务」。

### 4.2 对外开放 API

系统提供标准 HTTP 开放接口（`/api/v3`），供企业其他系统集成知识问答能力。

- Bearer Token 鉴权（API Key），按月配额计费，超额自动限流（返回 429）。
- API Key 可绑定指定知识库范围，精确控制可调用的数据边界。
- 每次成功调用计入用量，可在后台追踪。

### 4.3 钉钉机器人

支持把知识库接入钉钉，员工在钉钉群内 @ 机器人即可问答。

- 后台可配置多个机器人，每个机器人拥有专属回调地址，按 token 路由。
- 管理员在「钉钉机器人」页完成配置即可上线，无需改代码。

### 4.4 多渠道接入汇总

| 渠道 | 适用场景 |
|:-----|:---------|
| Web 端 | 日常知识问答、知识库管理、后台运营 |
| 语音通话 | 免手操作、客服、培训陪练 |
| 钉钉机器人 | 员工在办公 IM 内即时问答 |
| 开放 API | 企业自有系统集成 |
| MCP 协议 | AI 客户端 / IDE 插件 / 自研 Agent 调用 |

---

## 五、知识质量运营

系统提供一套完整的「问答质量闭环」，让知识库越用越聪明。

### 5.1 用户反馈

- 用户对每条回答可点赞 / 点踩并提交纠正意见。
- 审核员在「反馈审核」页集中处理，支持拒绝、收录。

### 5.2 经验库（Golden Pair）

- 把高质量问答对沉淀为标准答案库，命中后直接复用，提升一致性与速度。
- 支持管理员手填、从用户反馈一键生成、从高频问答自动蒸馏候选（待审批）。
- 统计标准答案总数与累计命中次数。

### 5.3 问答排行

- 按归一化问题聚合统计提问频次，支持全系统 / 按知识库 / 按用户三个维度。
- 配合经验库，快速识别高频问题并固化标准答案。

### 5.4 数据大屏与统计

- 可视化数据大屏，集中展示问答量、知识库规模、活跃度等核心指标。
- 历史对话支持全文搜索与回看。
- 对话可导出 Markdown（含来源与推理链）。

---

## 六、用户、组织与权限

### 6.1 用户与认证

- 注册 / 登录 / 忘记密码（邮箱验证码）。
- 注册支持邀请码机制与注册二维码。
- 基于 JWT 的安全认证。
- 用户画像（角色、领域、偏好），参与回答个性化组装。

### 6.2 组织与职位

- 维护部门（支持层级）与职位。
- 知识库 ACL 可按部门或职位授权，部门授权支持上级继承，便于大型组织的批量权限管理。

### 6.3 角色权限

| 角色 | 主要权限 |
|:-----|:---------|
| 管理员（admin） | 全部功能：知识库、用户、组织、配置、计费、合规、运营 |
| 审核员（auditor） | 反馈审核、经验库、问答排行、用量与对账查看、审计日志、脱敏配置查看 |
| 普通用户（user） | 问答、调研、教练、语音、个人中心、按授权访问知识库 |

---

## 七、安全与合规

面向政企与受监管行业，系统内置完整的安全合规能力。

### 7.1 PII 脱敏

- 可配置的个人敏感信息脱敏规则，输入文本自动识别并脱敏。
- 管理员可在线测试脱敏效果（输入文本即时返回脱敏结果）。

### 7.2 审计日志

- 关键操作全程留痕，记录操作人、动作、对象、状态、时间。
- 支持按用户、动作、对象类型、状态、时间范围分页检索。
- 支持导出 CSV，满足合规审计取证需要。

### 7.3 数据安全

- 全私有化部署，数据不出企业边界。
- 越权检索物理隔离，权限继承自知识库级 ACL。
- 全栈开源组件，零厂商锁定。

---

## 八、成本与计费

系统提供端到端的 AI 成本可视与对账能力，解决「AI 用量花了多少、花在谁身上」的运营难题。

### 8.1 用量统计

- 每个用户可查看本月用量。
- 管理员可逐用户钻取：区间汇总、每日明细、本月成本 Top 排行。
- 模型定价（含文本、视频、语音合成、音色复刻）内置可维护。

### 8.2 账单对账

- 支持与阿里云 BSS 真实账单按日对账，确保系统内记账与云厂商实际扣费一致。
- 管理员可手动触发某一天的对账，查看最近若干天的对账记录与配置状态。

### 8.3 API 配额计费

- 对外 API Key 按月配额计费，超额自动限流。
- 库级 API Key 支持按月配额，可随时吊销。

---

## 九、运维与配置

### 9.1 AI 配置中心

- 全部 RAG / 大模型 / 缓存 / 安全参数存于数据库，通过后台实时调整，无需重启、无需改代码。
- 可调参数包括：向量检索 TopK、融合常数、重排 TopK、安全阈值、缓存策略、模型温度、语音 VAD 参数等。

### 9.2 大模型 Provider 管理

- 支持配置多个大模型 Provider（兼容 OpenAI 协议，覆盖通义千问 / OpenAI / 任意兼容模型）。
- 一键切换激活 Provider，自动重建模型连接。
- 连通性测试：可用已保存或未保存的临时配置测试可达性。
- API Key 在列表中脱敏显示。

### 9.3 MCP 控制台

- MCP 工具状态管理、连通性测试、调用统计集中可视。

### 9.4 API Key 管理

- 颁发、查看、吊销对外 / 库级 API Key，配置配额与可访问知识库范围。

---

## 十、技术规格

### 技术栈

| 层级 | 技术选型 |
|:-----|:---------|
| 后端框架 | Spring Boot 3.4 + Spring AI 1.1.x |
| AI 模型层 | Spring AI OpenAI 兼容（DashScope / OpenAI / 任意 OpenAI 协议模型） |
| 向量数据库 | Milvus 2.x（COSINE 相似度，1024 维） |
| 全文检索 | Apache Lucene 8.11（中文分词 + BM25） |
| 重排模型 | gte-rerank（Cross-Encoder 精排） |
| 嵌入模型 | text-embedding-v3（1024 维） |
| 缓存与记忆 | Redis 7（热点缓存 + 长期记忆） |
| 元数据存储 | MySQL 8 + MyBatis Plus |
| 对象存储 | MinIO（S3 兼容） |
| MCP 服务 | Spring AI MCP Server（HTTP / SSE） |
| 实时通信 | SSE 流式输出 + WebSocket（语音） |
| 安全认证 | Spring Security + JWT |
| 前端 | Vue 3.5 + TypeScript + Vite + Element Plus + ECharts |

### 部署依赖

| 组件 | 版本 | 用途 |
|:-----|:-----|:-----|
| JDK | 17+ | 后端运行时 |
| Node.js | 20.19+ / 22.12+ | 前端构建 |
| MySQL | 8.x | 元数据存储 |
| Redis | 7.x | 缓存 + 长期记忆 |
| Milvus | 2.x | 向量检索 |
| MinIO | 最新版 | 文档对象存储 |
| 大模型 | 兼容 OpenAI 协议 | 对话 / 改写 / 审查 |
| 嵌入模型 | text-embedding-v3 | 文档向量化 |
| 重排模型 | gte-rerank | 结果重排 |

### 部署方式

- 提供 Docker Compose 一键部署（开发 / 生产两套编排）。
- 数据库 schema 走幂等迁移脚本，全新部署自动初始化，老库升级一行命令完成，重复执行安全。
- 支持物理机、内网、专有云等多种私有化环境。

---

## 十一、功能清单速览

**终端用户**

- 智能问答（流式、溯源、追问、范围限定、技能包、反馈）
- Agent 调研（多智能体协作、推理链回放、通信图谱）
- 教练模式（自动出题、评分、个人/团队统计）
- 实时语音通话（VAD 打断、多音色、联网切换）
- 历史对话搜索与导出
- 个人中心与用户画像

**知识管理**

- 知识库两层架构（可见性、ACL、API Key）
- 全格式文档上传（并发、分片、文件夹拖拽、失败重试）
- 智能处理流水线（多模态、时间戳、并发向量化）
- 文档归类与权限继承

**运营与质量**

- 反馈审核、经验库、问答排行
- 数据大屏与统计

**管理后台**

- 用户管理、组织与职位、知识库 ACL
- AI 配置中心、大模型 Provider 管理、Soul 人格、技能包、教练规则
- MCP 控制台、API Key 管理、邀请码、钉钉机器人

**安全合规与计费**

- PII 脱敏、审计日志
- 用量统计、账单对账、API 配额计费

**开放能力**

- MCP 标准协议、对外开放 API、钉钉机器人接入

---

## 十二、服务与授权

本产品为商业授权软件，支持私有化部署、定制开发与技术支持。如需授权、试用、定制或技术支持，请联系产品供应方。

---

*本说明书描述的功能基于商用授权版当前能力，具体以实际交付版本为准。*
