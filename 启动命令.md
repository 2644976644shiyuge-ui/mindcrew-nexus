# MindCrew 自建部署启动命令

以下命令需要在项目目录（`MindCrew/MindCrew`）中执行。

## 部署前准备

确认服务器已准备好 `.env.selfhost`：

```bash
cp .env.selfhost.example .env.selfhost
vi .env.selfhost
chmod +x deploy.selfhost.sh sql/*.sh
```

`.env.selfhost` 包含数据库密码、模型 API Key 和 JWT 密钥，请勿提交到 Git 或发送给其他人。

### 腾讯云 CVM 镜像加速（大陆地域建议配置）

如果 `./deploy.selfhost.sh init` 报 `registry-1.docker.io` 连接超时，并伴随 `milvus Error` 或多个 `context canceled`，先配置腾讯云内网镜像源：

```bash
mkdir -p /etc/docker
[ ! -f /etc/docker/daemon.json ] || cp /etc/docker/daemon.json /etc/docker/daemon.json.bak

cat > /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com"
  ],
  "max-concurrent-downloads": 3
}
EOF

systemctl daemon-reload
systemctl restart docker
docker info | grep -A 5 "Registry Mirrors"
```

> 该镜像源仅支持腾讯云内网。如 `/etc/docker/daemon.json` 原本有其他重要配置，请从 `.bak` 备份手动合并，不要丢弃原配置。

配置生效后逐个拉取基础镜像：

```bash
docker pull mysql:8.0
docker pull redis:7-alpine
docker pull minio/minio:latest
docker pull milvusdb/milvus:v2.3.4
docker pull quay.io/coreos/etcd:v3.5.5
```

Docker 会复用之前已下载的镜像层，无需清理。拉取完成后再执行 `./deploy.selfhost.sh init`。详细故障说明见 [部署方案.md](部署方案.md#腾讯云-cvmdocker-hub-拉取超时处理)。

## 首次部署

```bash
./deploy.selfhost.sh init
```

该命令会检查或安装 Docker，构建镜像，启动 MySQL、Redis、Milvus、MinIO、后端和前端，并自动执行数据库迁移。首次执行通常需要 5～10 分钟。

## 日常启动

服务器重启或容器停止后，使用现有镜像启动全部服务：

```bash
./deploy.selfhost.sh up
```

此命令不会重新构建镜像，但会自动检查数据库迁移。

## 更新代码后重新部署

```bash
./deploy.selfhost.sh update
```

该命令会重新构建后端和前端，执行数据库迁移，并滚动重启服务。

## 查看运行状态

```bash
./deploy.selfhost.sh status
```

## 查看日志

默认查看后端日志：

```bash
./deploy.selfhost.sh logs
./deploy.selfhost.sh logs backend
```

也可以查看其他服务：

```bash
./deploy.selfhost.sh logs frontend
./deploy.selfhost.sh logs mysql
./deploy.selfhost.sh logs redis
./deploy.selfhost.sh logs milvus
./deploy.selfhost.sh logs minio
```

按 `Ctrl+C` 退出日志查看，不会停止服务。

## 重启服务

```bash
./deploy.selfhost.sh restart backend
./deploy.selfhost.sh restart frontend
./deploy.selfhost.sh restart mysql
```

未指定服务时默认重启后端：

```bash
./deploy.selfhost.sh restart
```

## 停止全部服务

```bash
./deploy.selfhost.sh stop
```

该命令只停止并删除容器，MySQL、Milvus 和上传文件等数据卷会保留。之后可以执行 `up` 恢复。

## 清空全部服务和数据

```bash
./deploy.selfhost.sh nuke
```

> 警告：该命令会删除全部容器和数据卷，包括 MySQL 数据、Milvus 向量数据及上传文件，无法恢复。执行时还需要输入 `yes` 二次确认。

## 常用操作速查

| 命令 | 用途 |
| --- | --- |
| `./deploy.selfhost.sh init` | 首次构建并启动全部服务 |
| `./deploy.selfhost.sh up` | 使用现有镜像启动全部服务 |
| `./deploy.selfhost.sh update` | 更新代码后重新构建并重启 |
| `./deploy.selfhost.sh status` | 查看容器状态 |
| `./deploy.selfhost.sh logs [服务名]` | 持续查看服务日志 |
| `./deploy.selfhost.sh restart [服务名]` | 重启指定服务 |
| `./deploy.selfhost.sh stop` | 停止服务并保留数据 |
| `./deploy.selfhost.sh nuke` | 删除全部服务及数据 |

## 访问地址

当前 `docker-compose.yml` 暴露的访问地址为：

- 前端：`http://服务器IP:8085`
- 后端：`http://服务器IP:8090`
- MinIO API：`http://服务器IP:9000`
- MinIO 控制台：`http://服务器IP:9001`

若服务启动异常，依次执行：

```bash
./deploy.selfhost.sh status
./deploy.selfhost.sh logs backend
./deploy.selfhost.sh logs frontend
```
