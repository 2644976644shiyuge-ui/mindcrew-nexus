# MindCrew 环境要求

> 面向部署 / 运维 / 集成的技术人员。所有版本以代码库为准（`pom.xml` / `package.json` / `docker-compose.yml`），
> 本表于 2026-06-20 核对。标准部署为**全容器化**：宿主机仅需 Docker，其余组件随容器启动；
> 生产可将 MySQL / Redis / 对象存储替换为云托管实例。

## 1. 运行时组件（必需）

| 组件 | 最低要求 | 项目默认 | 约束来源 / 说明 |
|------|----------|----------|-----------------|
| **JDK** | **17**（LTS） | eclipse-temurin **17** | Spring Boot 3.x 基线为 Java 17；字节码 target=17，运行 JRE 必须 ≥17。**不支持 8 / 11**。可升至 21。 |
| **MySQL** | **8.0**（如需降级见 §5） | `mysql:8.0` | 需 `utf8mb4`；中文全文检索使用 **ngram 全文索引**。字符集排序统一 `utf8mb4_unicode_ci`。 |
| **Redis** | **6**（5 已 EOL，见 §5） | `redis:7-alpine` | 仅使用 String + ZSet，无 Streams / 模块依赖。 |
| **Milvus** | **2.3.x** | `milvusdb/milvus:v2.3.4` | 向量检索（COSINE，1024 维）。依赖 etcd `v3.5.5` + 对象存储。 |
| **对象存储** | S3 兼容 | MinIO（容器）/ 阿里云 OSS | 二选一：自建用 MinIO，生产建议 OSS。存放原始文档与音视频。 |

## 2. 宿主机（容器化部署）

| 项 | 要求 |
|----|------|
| Docker Engine | ≥ 24（支持 Compose v2） |
| Docker Compose | v2（`docker compose`） |
| 其余（JDK/Node/MySQL/Redis/Milvus） | **无需在宿主机安装**，全部随容器运行 |

## 3. 构建工具链（仅从源码构建时需要）

| 端 | 工具 | 版本 |
|----|------|------|
| 后端 | Maven | **3.9** + JDK 17 |
| 前端 | Node.js | **≥ 20.19**（或 ≥ 22.12，见 `package.json` engines） |
| 前端 | 包管理 | npm（含 `package-lock.json`，构建用 `npm ci`） |

> 镜像内已完成构建（多阶段 Dockerfile）；使用预构建镜像时无需本地工具链。

## 4. 核心依赖版本（技术栈）

**后端**：Spring Boot **3.3.5** · Spring AI **1.1.4**（OpenAI 兼容协议）· MyBatis-Plus **3.5.7** ·
jjwt **0.12.5** · fastjson2 **2.0.47** · hutool **5.8.26** · MinIO Client **8.5.7** · Apache POI（ooxml/scratchpad） · milvus-sdk-java。

**前端**：Vue **3.5** · Vite **7** · TypeScript **5.9** · Element Plus **2.13** · ECharts **6** · Pinia **3** · 构建产物由 `nginx:1.27-alpine` 托管。

## 5. 兼容性与可降级范围

| 组件 | 可降级到 | 前提 / 风险 |
|------|----------|-------------|
| MySQL | **5.7.8+** | ngram 全文索引（5.7.6+）、JSON 类型（5.7.8+）、`utf8mb4_unicode_ci` 均支持。**MySQL 5.7 已于 2023-10 EOL，不建议生产使用**；降级前须用目标实例完整执行 `sql/` 全量迁移验证。 |
| Redis | **5+** | 仅用基础数据结构，无版本特性依赖。**Redis 5 已 EOL，建议 6+**。仅需修改镜像 tag。 |
| JDK | 17 → 21 | Spring Boot 3.3 官方支持 21；同步修改 `pom.xml` 与 Docker 镜像 tag。 |

## 6. 系统级依赖（后端容器内已内置）

非容器化自建后端时需在宿主机安装：

| 依赖 | 用途 |
|------|------|
| **FFmpeg** | 音视频切片 / 转写预处理 |
| **LibreOffice** | Office 文档（doc/ppt 等）转换抽取 |
| 中文字体 | 文档渲染 / 转换的中文显示 |

## 7. 资源与网络（生产建议）

- **最低**：8C16G（单机全栈，`JAVA_OPTS -Xmx2g`）；**推荐**：应用与 MySQL/Redis/Milvus 分离部署。
- **安全**：MySQL / Redis / Milvus / MinIO 的端口**禁止对公网开放**，仅内网或 VPC 白名单访问。
- 外呼：需可访问所配置的 LLM/Embedding 服务（DashScope / OpenAI 兼容端点）。

---

*权威来源：`pom.xml`、`MindCrew-frontend/package.json`、`docker-compose.yml`、后端 `Dockerfile`。本文档与代码不一致时以代码为准并请同步修订。*
