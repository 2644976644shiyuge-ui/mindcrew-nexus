# MindCrew 云服务器部署方案

适用版本：商用授权版
文档性质：运维部署手册
更新日期：2026-07-22

> 📋 **部署前先看 [环境要求.md](环境要求.md)**（JDK / MySQL / Redis 等版本与可降级范围）。
> 第三方开源许可见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。

本文档提供两套完整的云服务器部署方案，均基于 Docker / Docker Compose，覆盖从资源准备、环境变量、执行命令到验证运维的全流程。

---

## 〇、本地开发 / 快速跑通（5 分钟）

仅需 Docker Desktop（JDK / Node / MySQL / Redis / Milvus 全在容器内，无需本机安装）：

```bash
cd MindCrew/                  # 项目根目录
cp .env.example .env          # 至少填 BAILIAN_API_KEY=sk-xxx
docker compose up -d --build  # 首次约 5-10 分钟（拉镜像 + 构建）
```

完成后访问 `http://localhost:8085`，用默认管理员 **admin / 666666** 登录（首次登录后请改密）。
常用：`docker compose logs -f backend`（日志）· `docker compose down`（停，保留数据）· `docker compose down -v`（清空含数据卷）。

> 生产部署请走下面的方案 A / B，**不要**直接用本地开发的 `docker compose` 上生产。

---

## 一、方案选型

| 维度 | 方案 A · 全容器自建（推荐） | 方案 B · 托管中间件 |
|:--|:--|:--|
| 适用场景 | 单台云服务器 / 通用 VPS，全套自己跑 | 阿里云 ECS + 托管 RDS / Tair |
| MySQL | 容器内自建 MySQL 8 | 外部阿里云 RDS MySQL |
| Redis | 容器内 Redis 7 | 外部阿里云 Tair |
| 对象存储 | 阿里云 OSS（业务文件） | 阿里云 OSS（业务文件） |
| 向量库 Milvus / MinIO | 容器内 | 容器内 |
| 部署脚本 | `deploy.selfhost.sh` | `deploy.sh` |
| 编排文件 | `docker-compose.yml` | `docker-compose.prod.yml` |
| 环境变量文件 | `.env.selfhost` | `.env.prod` |
| 后端健康检查端口 | 8090 | 8080 |
| 数据库初始化 | 部署脚本**自动建表+迁移** | 部署脚本**自动建表+迁移** |

**如何选**：

- 只有一台云服务器、想最省事一次跑通 —— 选 **方案 A**。MySQL / Redis / Milvus / MinIO 全部容器化，只需外挂一个 OSS。
- 已有阿里云 RDS、Tair 等托管中间件，要数据与计算分离、便于独立扩容备份 —— 选 **方案 B**。

> 两套方案完全独立：各自的脚本、compose、env 互不读取，不会串。

> **数据库迁移已全自动**（两方案通用）：`deploy.selfhost.sh` 与 `deploy.sh` 在每次 `init` 和 `update` 时都会自动执行数据库迁移——
> 空库自动建全部表，已有数据的库只补缺失的列/新表（幂等、不动数据、不丢数据）。
> SQL 文件**自动发现**，以后往 `sql/` 目录新增任何 `.sql` 都会被自动纳入，**无需手动导库、无需改任何脚本**。

---

## 二、服务器与资源准备（两方案通用）

### 2.1 服务器要求

| 项目 | 最低 | 推荐 |
|:--|:--|:--|
| CPU | 4 核 | 8 核 |
| 内存 | 8 GB | 16 GB |
| 磁盘 | 50 GB | 100 GB SSD |
| 系统 | Linux（CentOS / Ubuntu / 阿里云 Linux 等） | 同左 |
| 权限 | root 或具备 sudo | 同左 |
| 网络 | 能访问外网拉取镜像与调用大模型 API | 同左 |

> 内存说明：Milvus 向量库、MySQL、后端 JVM 同机运行较吃内存。方案 A 下 8G 机器建议 `JAVA_OPTS` 的 `-Xmx` 设为 2g；16G 机器可设 4g。

### 2.2 安全组 / 防火墙放行

| 端口 | 服务 | 是否对公网开放 |
|:--|:--|:--|
| 8085 | 前端 Web 入口（方案 A） | 开放 |
| 80 / 自定义 | 前端 Web 入口（方案 B，见 compose） | 开放 |
| 8090（A）/ 8080（B） | 后端 API / MCP / 健康检查 | 仅对外集成时开放 |
| 3309 / 6381 / 19530 / 9000 / 9001 | MySQL / Redis / Milvus / MinIO | **禁止对公网开放**，仅本机内部使用 |

安全建议：生产环境用 Nginx / SLB 在前面做 HTTPS 反向代理，仅暴露 443，后端与中间件端口全部锁在内网。

### 2.3 必备的阿里云资源

1. **百炼 API Key**（必需）
   - 申请：https://bailian.console.aliyun.com
   - 用途：对话生成、嵌入（text-embedding-v3）、重排（gte-rerank）、语音识别与合成全部依赖。
   - 取 `sk-` 开头的 Key。

2. **OSS Bucket + RAM AccessKey**（必需）
   - 控制台 → OSS 创建 Bucket，地域与服务器一致。
   - RAM 控制台创建子账号，授权 `AliyunOSSFullAccess`，得到 AccessKey / SecretKey。
   - 用途：原始文档、音视频持久化；视频 / 音频 RAG 需要 OSS 公网可达 URL。

3. **联网搜索 Key**（可选）
   - 百度千帆 AI 搜索：https://console.bce.baidu.com/qianfan/ （`BAIDU_SEARCH_API_KEY`，默认搜索源）
   - 或 Tavily：https://app.tavily.com （`TAVILY_API_KEY`，免费 1000 次/月，备用）
   - 不填则对话框「联网」开关搜不到任何结果。

4. **阿里云账单对账 RAM AK**（可选，方案 B 常用）
   - 授权 `AliyunBSSReadOnlyAccess`，用于定时拉取真实账单校准成本估算。

5. **方案 B 额外需要**：阿里云 RDS MySQL 实例、阿里云 Tair（Redis）实例，记下内网地址、端口、账号、密码。

---

## 三、方案 A · 全容器自建（推荐）

MySQL / Redis / Milvus / MinIO 全部容器化，业务文件存阿里云 OSS。数据通过 Docker named volume 持久化。

### 3.1 部署步骤

```bash
# 1. 上传工程到服务器（本地机器执行）
scp -r MindCrew/ root@<服务器IP>:/data/

# 2. 登录服务器，进入含部署脚本的目录
ssh root@<服务器IP>
cd /data/MindCrew/MindCrew

# 3. 生成环境变量文件并填写
cp .env.selfhost.example .env.selfhost
vi .env.selfhost                 # 按 3.2 填写

# 4. 一键部署：自动装 Docker → 自动建库 → 构建镜像 → 启动全部服务
#    首次约 5-10 分钟（构建前后端 + 拉中间件镜像）
./deploy.selfhost.sh init
```

#### 腾讯云 CVM：Docker Hub 拉取超时处理

中国大陆地域的腾讯云 CVM 首次执行 `./deploy.selfhost.sh init` 时，可能出现：

```text
Get "https://registry-1.docker.io/v2/": Client.Timeout exceeded
milvus Error ...
redis/mysql/minio Error context canceled
```

这是服务器访问 Docker Hub 超时，不是 `.env.selfhost` 或业务配置错误。`context canceled` 是其中一个镜像拉取失败后的连带取消。

腾讯云 CVM 可使用腾讯云内网 Docker Hub 镜像源（该地址仅支持腾讯云内网）：

```bash
# 如已有 Docker daemon 配置，先备份；需保留的原有选项应手动合并，不要直接丢弃
mkdir -p /etc/docker
[ ! -f /etc/docker/daemon.json ] || cp /etc/docker/daemon.json /etc/docker/daemon.json.bak

cat > /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com"
  ],
  "max-concurrent-downloads": 3
}
EOF

systemctl daemon-reload
systemctl restart docker
docker info | grep -A 5 "Registry Mirrors"
```

验证输出中出现 `https://mirror.ccs.tencentyun.com/` 后，可先逐个拉取项目依赖的基础镜像，便于定位具体失败项：

```bash
docker pull mysql:8.0
docker pull redis:7-alpine
docker pull minio/minio:latest
docker pull milvusdb/milvus:v2.3.4
docker pull quay.io/coreos/etcd:v3.5.5
```

`mirror.ccs.tencentyun.com` 主要加速 Docker Hub；`quay.io/coreos/etcd` 不经过该镜像源，如中断可重复执行对应 `docker pull`。Docker 会复用已下载的镜像层，无需先清理。

> 后端 `Dockerfile` 已默认将 Ubuntu APT 切到腾讯云公网镜像，Maven Central 也通过腾讯云公共仓库下载，避免构建时长时间停在 LibreOffice 或 Java 依赖下载。如需使用其他 Ubuntu 镜像，可在构建时传入 `--build-arg UBUNTU_MIRROR=<镜像地址>` 覆盖。

所有基础镜像拉取成功后重新执行：

```bash
./deploy.selfhost.sh init
```

> 非腾讯云服务器不要直接套用这个内网镜像源，应使用所在云厂商提供的镜像加速地址。

### 3.2 `.env.selfhost` 配置项

```bash
# ── 大模型（必填）──
BAILIAN_API_KEY=sk-你的百炼key

# ── 对象存储：业务文件存阿里云 OSS（必填）──
STORAGE_TYPE=oss
OSS_ENDPOINT=https://oss-cn-beijing.aliyuncs.com   # 必须与 Bucket 同地域
OSS_REGION=cn-beijing
OSS_BUCKET=你的bucket名称
OSS_ACCESS_KEY=你的AccessKey
OSS_SECRET_KEY=你的SecretKey

# ── 自建 MySQL root 密码（必改成强密码）──
MYSQL_ROOT_PASSWORD=换成强密码

# ── Redis 密码（可选，留空=无密码）──
REDIS_PASSWORD=

# ── 加密主密钥（必改 · 32 字符以上随机串）──
# 生成：openssl rand -base64 32
# ⚠ 上线投入使用后绝不能再改，否则 LLM Provider 等加密配置全部失效
CRYPTO_MASTER_KEY=请填入随机32位以上字符串

# ── 联网搜索（可选）──
WEB_SEARCH_PROVIDER=baidu          # baidu(默认) / tavily
BAIDU_SEARCH_API_KEY=
TAVILY_API_KEY=
DOCMIND_WEB_SEARCH_ENABLED=true    # 联网总开关，false 则全局禁用联网

# ── 构建加速（可选）──
# 腾讯云 CVM 建议使用内网 Ubuntu 镜像；不填则默认走腾讯云公网镜像
UBUNTU_MIRROR=http://mirrors.tencentyun.com/ubuntu

# ── JVM（按服务器内存调整）──
JAVA_OPTS=-Xms512m -Xmx2048m -XX:+UseG1GC

# ── 前端端口环境变量（compose 实际映射见 3.4 说明）──
FRONTEND_PORT=80
```

> **数据库自动初始化与迁移**：`./deploy.selfhost.sh init` 会在 MySQL 就绪后自动执行迁移——全新库建全部表，已有库只补缺失的列/新表（幂等、不丢数据）。SQL 文件自动发现，无需手动操作。后续每次 `update` 也会自动补齐增量。

### 3.3 验证

```bash
./deploy.selfhost.sh status                 # 所有容器应为 Up / healthy
curl http://localhost:8090/actuator/health  # 后端健康检查，期望 {"status":"UP"}
./deploy.selfhost.sh logs backend           # 观察后端启动日志无报错
```

浏览器访问前端：

```
http://<服务器IP>:8085
```

> **注意端口**：方案 A 的 `docker-compose.yml` 把前端映射到主机 **8085** 端口（容器内 80）。脚本结尾打印的 `http://<ip>` 省略了端口号，实际以 **8085** 为准。

#### 默认登录账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| `admin` | `666666` | 管理员（全部权限） |

> ⚠️ **首次登录后请立即在「个人中心」修改密码**。该账号由 `sql/docmind-init.sql` 在全新库初始化时种入；生产环境务必改密。
> 登录后请到「大模型 Provider / AI 配置」确认**对话模型**与**嵌入模型**均可用（嵌入模型不可用会导致知识入库/检索静默失败）。

### 3.4 方案 A 端口映射（主机:容器）

| 服务 | 主机端口 | 容器端口 | 说明 |
|:--|:--|:--|:--|
| frontend | 8085 | 80 | Web 入口 |
| backend | 8090 | 8080 | API / MCP / 健康检查 |
| mysql | 3309 | 3306 | 自建数据库 |
| redis | 6381 | 6379 | 缓存 / 长期记忆 |
| milvus | 19530 / 9091 | 19530 / 9091 | 向量检索 |
| minio | 9000 / 9001 | 9000 / 9001 | Milvus 数据底座（非业务存储） |

### 3.5 后续更新（改了代码 / 知识库后）

```bash
cd /data/MindCrew/MindCrew
git pull                       # 拉最新代码
./deploy.selfhost.sh update    # 重新构建 backend + frontend，滚动重启，自动补齐数据库字段
```

> `update` 会同时重建前后端镜像并自动执行数据库迁移（幂等），新增的字段/表自动补齐，不会动已有数据。MySQL / Redis / Milvus / MinIO 容器不受影响，持续运行。
>
> ⚠️ **注意**：`update` 用了 `--no-deps`，**只重启 backend/frontend，不会拉起基础设施**（mysql/redis/milvus 等）。若基础设施已掉线（见 3.6），先 `up` 再 `update`。

### 3.6 命令速查 & 开机 / 掉线恢复

| 命令 | 用途 |
|:--|:--|
| `./deploy.selfhost.sh up` | **用现有镜像启动全部服务（含基础设施），不重建**。服务器重启后、或 mysql/milvus 等容器掉线后，一键拉起 |
| `./deploy.selfhost.sh update` | 改了代码后重新构建并滚动重启 backend/frontend（前提：基础设施在跑） |
| `./deploy.selfhost.sh init` | 全新机器首次部署（装 Docker + 构建全套 + 启动） |
| `./deploy.selfhost.sh status` / `logs [svc]` / `restart [svc]` / `stop` | 状态 / 日志 / 重启单服务 / 停止（数据保留） |

**典型故障**：后端日志报 `java.net.UnknownHostException: mysql` → 是基础设施容器掉线了（`mysql` 容器没在跑，主机名解析不到），不是配置错误。

- **Linux 服务器**：所有容器配了 `restart: unless-stopped`，且 `init` 已设 Docker 开机自启 → **服务器重启后自动全部拉回**，通常无需人工干预。
- **本地 Mac（Docker Desktop）**：Docker Desktop 停止/内存不足会杀掉容器（退出码 137）。重启电脑后：① 先打开 Docker Desktop 等就绪 → ② `./deploy.selfhost.sh up`。
- **内存不足导致反复 137**：调大可用内存（Docker Desktop → Settings → Resources；服务器确保 ≥8GB），否则 OOM-killer 会优先杀 mysql/milvus。

### 3.7 从本地远程给客户部署（SSH 直连）

人在自己电脑前，把代码推到客户 Linux 服务器并一键部署：

```bash
# 首次：先把代码推上去（会提示去服务器配 .env.selfhost），配好密钥后再跑一次
REMOTE=root@客户IP ./remote-deploy.sh init
# 日常更新
REMOTE=root@客户IP ./remote-deploy.sh update
# 可选参数：REMOTE_DIR=/opt/mindcrew SSH_PORT=2222
```

> `remote-deploy.sh` 做的事：rsync 同步源码（排除 node_modules/target 等，**不覆盖服务器上的 .env.selfhost**）→ 在服务器本机执行 `deploy.selfhost.sh`。**在服务器本机构建（amd64），天然规避 Mac(arm64) 跨架构镜像问题**。密钥按客户单独在服务器上配。

---

## 四、方案 B · 托管中间件（阿里云 ECS + RDS + Tair）

数据库用阿里云 RDS MySQL，缓存用阿里云 Tair，业务文件存 OSS。Milvus / MinIO 仍容器化，持久化数据落在 `/data/mindcrew/` 宿主机目录。

### 4.1 部署步骤

```bash
# 1. 上传工程到 ECS（本地机器执行）
scp -r MindCrew/ root@<ECS_IP>:/data/

# 2. 登录 ECS，进入部署目录
ssh root@<ECS_IP>
cd /data/MindCrew/MindCrew

# 3. 在 RDS 里先建好空库（仅这一步需手动 · 一次性）
#    控制台 DMS 或客户端执行：
#    CREATE DATABASE docmind DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 4. 生成环境变量文件并填写
cp .env.prod.example .env.prod
vi .env.prod                     # 按 4.2 填写（MYSQL_HOST/USER/PASSWORD 等）

# 5. 一键部署（自动对 RDS 建表+迁移，再启动服务）
./deploy.sh init
```

### 4.2 `.env.prod` 配置项

```bash
# ── 镜像 tag（CI 推送时按 commit 替换，手动部署保持 latest）──
IMAGE_TAG=latest

# ── 阿里云 RDS MySQL（控制台 → RDS → 实例详情 → 内网地址）──
MYSQL_HOST=rm-bp1xxxx.mysql.rds.aliyuncs.com
MYSQL_PORT=3306
MYSQL_USER=mindcrew                 # 建议创建专用账号，勿用 root
MYSQL_PASSWORD=强密码（≥16位混合字符）

# ── 阿里云 Tair Redis（控制台 → Tair → 实例详情 → 内网地址）──
REDIS_HOST=r-bp1xxxx.redis.rds.aliyuncs.com
REDIS_PORT=6379
REDIS_PASSWORD=Redis密码

# ── 阿里云 OSS（必填，视频/音频 RAG 需公网可达 URL）──
OSS_ENDPOINT=https://oss-cn-beijing.aliyuncs.com   # 与 Bucket 同地域
OSS_REGION=cn-beijing
OSS_BUCKET=mindcrew-prod
OSS_ACCESS_KEY=LTAI5xxxx
OSS_SECRET_KEY=OSS密钥
OSS_CUSTOM_DOMAIN=                  # 可选，CDN 加速域名

# ── Milvus 内置 MinIO 凭证（Milvus 自用数据底座，与业务存储无关）──
MILVUS_MINIO_USER=mindcrew_milvus
MILVUS_MINIO_PASSWORD=强密码

# ── 大模型 ──
BAILIAN_API_KEY=sk-你的百炼key

# ── 联网搜索（可选）──
WEB_SEARCH_PROVIDER=baidu
BAIDU_SEARCH_API_KEY=
TAVILY_API_KEY=
DOCMIND_WEB_SEARCH_ENABLED=true

# ── 安全密钥（生产强制）──
# CRYPTO_MASTER_KEY 上线后绝不能改；生成：openssl rand -base64 48
CRYPTO_MASTER_KEY=至少32位随机串
# JWT 签名密钥；生成：openssl rand -hex 32
JWT_SECRET=64位hex随机串

# ── 阿里云账单对账（可选，需 AliyunBSSReadOnlyAccess 子账号 AK）──
BSS_ENABLED=false                   # true 才启用每日定时对账
BSS_ACCESS_KEY=
BSS_SECRET_KEY=
BSS_PRODUCT_CODES=dashscope         # 逗号分隔，dashscope=百炼，可加 oss
BSS_ALERT_THRESHOLD=0.10            # 估算与真实差异超 10% 写告警日志

# ── 语音播报情绪（可选，开启前先验证 cosyvoice-v2 接受 emotion 参数）──
TTS_EMOTION_ENABLED=false

# ── JVM 调优（4C8G ECS 推荐）──
JAVA_OPTS=-Xms2g -Xmx5g -XX:+UseG1GC -XX:MaxRAMPercentage=70.0 -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs/heap-dump.hprof
```

### 4.3 RDS 建表与迁移（已全自动）

**无需再手动导库。** `./deploy.sh init` / `update` 会自动对 RDS 执行迁移：

- 空库（仅建过 `docmind` 库、无表）→ 自动建全部表 + 跑全部增量；
- 已有数据的库 → 只补缺失的列/新表，幂等、不动数据、不丢数据；
- SQL 文件自动发现，新增 `.sql` 自动纳入。

宿主机没有 `mysql` 客户端也没关系：脚本会用一次性 `mysql:8.0` 容器连 RDS。

唯一的手动前置：在 RDS 里先建好空库（4.1 第 3 步）：

```sql
CREATE DATABASE docmind DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

> 如需手动单独跑迁移（一般不必，部署脚本已自动执行）：
> ```bash
> MIGRATE_MODE=rds MIGRATE_BASELINE=auto \
>   MYSQL_HOST=<RDS地址> MYSQL_PORT=3306 \
>   MYSQL_USER=<账号> MYSQL_PASSWORD=<密码> MYSQL_DATABASE=docmind \
>   bash sql/migrate.sh
> ```

### 4.4 验证

```bash
./deploy.sh status
curl http://localhost:8080/actuator/health   # 方案 B 后端端口为 8080
./deploy.sh logs backend
```

前端访问端口以 `docker-compose.prod.yml` 中 frontend 服务的映射为准（默认 80，可在 compose / env 调整），即 `http://<ECS_IP>`。

---

## 五、日常运维命令（两方案命令同名）

方案 A 用 `./deploy.selfhost.sh`，方案 B 用 `./deploy.sh`，子命令一致：

```bash
<脚本> status              # 查看所有容器状态
<脚本> logs [服务名]        # 实时日志，默认 backend；可选 frontend/mysql/milvus/redis...
<脚本> restart [服务名]     # 重启单个服务
<脚本> update              # 改代码后：重新构建 backend+frontend 并滚动重启
<脚本> stop                # 停止所有容器（数据保留）
<脚本> nuke                # 删除全部容器 + 数据卷（不可逆，向量库/上传文件会丢，慎用）
```

示例：

```bash
./deploy.selfhost.sh logs backend       # 方案 A 看后端日志
./deploy.selfhost.sh update             # 方案 A 滚动更新
./deploy.sh restart frontend            # 方案 B 重启前端
```

### 升级数据库 SQL（通常无需手动）

每次 `./deploy.selfhost.sh update` / `./deploy.sh update` 都会**自动补齐**新增的列/表，正常情况下你什么都不用做。

如确需手动操作（方案 A · 自建 MySQL 容器）：

```bash
./sql/run.sh --migrate     # 补齐缺失的列/表 · 幂等 · 有数据的库安全（推荐）
./sql/run.sh --check       # 校验中文无乱码 / 字符集
./sql/run.sh voice-persona-schema.sql   # 单独跑某个文件
```

> ⚠ `./sql/run.sh --all` 是**全量重建**（含 `DROP TABLE`，会清空并重建基线表），且需输入 `yes` 确认——**仅用于全新或可清空的库**。线上补字段请始终用 `--migrate`。

`run.sh` 默认连接参数（可用环境变量覆盖）：

```bash
MYSQL_CONTAINER=mindcrew-mysql   # 容器名
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=docmind
```

> 工作原理：SQL 自动分两类——基线表（含 `DROP TABLE`，仅空库建一次）与增量（无 `DROP`，每次部署安全重跑）。文件全自动发现，新增 `.sql` 无需改任何脚本。详见 `sql/_sqlset.sh` / `sql/migrate.sh`。

---

## 六、数据持久化与备份

### 方案 A（Docker named volume）

数据卷：`mindcrew_mysql_data`、`mindcrew_redis_data`、`mindcrew_milvus_data`、`mindcrew_etcd_data`、`mindcrew_minio_data`、`mindcrew_uploads`、`mindcrew_logs`。

```bash
# 备份自建 MySQL
docker exec mindcrew-mysql mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --default-character-set=utf8mb4 docmind > backup_$(date +%F).sql

# 查看数据卷
docker volume ls | grep mindcrew
```

> 业务文件（文档 / 音视频）已存阿里云 OSS，由 OSS 侧负责冗余与备份。

### 方案 B（宿主机目录 + 托管服务）

- Milvus / MinIO 等持久化数据在 `/data/mindcrew/{etcd,milvus,milvus-minio,redis,uploads,logs}`。
- MySQL 数据在 RDS，由阿里云自动备份；Redis 在 Tair。
- 备份重点：RDS 快照 + OSS。

---

## 七、常见问题排查

| 现象 | 排查方向 |
|:--|:--|
| 前端打不开 | 安全组是否放行 8085（A）/ 80（B）；`<脚本> status` 看 frontend 是否 Up |
| 后端健康检查非 UP | `<脚本> logs backend`；检查 MySQL/Redis 连接、`BAILIAN_API_KEY` 是否正确 |
| 对话报错 / 无回答 | 百炼 Key 余额或权限；网络是否能访问百炼 API |
| 联网开关无效果 | 未填 `BAIDU_SEARCH_API_KEY` / `TAVILY_API_KEY`，或 `DOCMIND_WEB_SEARCH_ENABLED=false` |
| 上传文档失败 | OSS 配置（Endpoint 地域、AK/SK、Bucket 权限）；`STORAGE_TYPE=oss` |
| 视频/音频解析异常 | OSS URL 是否公网可达；Bucket 读权限 |
| 某功能页报表不存在 | 重新跑 `<脚本> update`（会自动补迁移）；或手动 `./sql/run.sh --migrate`（方案 A）/ 见 4.3 手动迁移命令（方案 B·RDS） |
| 加密配置失效 | 是否改过 `CRYPTO_MASTER_KEY`（不可改）；恢复原值 |
| 容器反复重启 | 内存不足，调小 `JAVA_OPTS -Xmx` 或升配 |

---

## 八、上线检查清单

部署完成后逐项确认：

- [ ] `<脚本> status` 全部容器 Up / healthy
- [ ] 后端 `/actuator/health` 返回 UP（A:8090 / B:8080）
- [ ] 浏览器可打开前端并注册 / 登录
- [ ] 上传一个文档，处理状态走到 ready，可被问答检索到
- [ ] 提问能正常流式回答并带来源标注
- [ ] 联网开关（如已配 Key）能搜到结果
- [ ] `CRYPTO_MASTER_KEY`、`MYSQL_ROOT_PASSWORD`/`MYSQL_PASSWORD` 已改强密码并妥善留存
- [ ] 中间件端口未对公网开放，前端建议加 HTTPS 反代
- [ ] 已配置数据库 / OSS 备份策略

---

## 九、关键提醒

1. **`CRYPTO_MASTER_KEY` 一旦上线投入使用绝不能修改**，否则数据库中加密存储的 LLM Provider Key 等配置全部失效。
2. 方案 A 前端实际端口是 **8085**，不是 80。
3. 方案 B 的 RDS 只需**先建好空库 `docmind`**，建表与迁移由 `./deploy.sh` 自动完成，无需手动导 SQL。
4. 数据库迁移已全自动：每次 `init`/`update` 自动补齐增量字段，新增 SQL 无需改脚本。补字段用 `./sql/run.sh --migrate`，**切勿用 `--all` 去升级线上库**（会清表）。
5. 中间件端口（MySQL / Redis / Milvus / MinIO）务必锁在内网，不要暴露公网。

---

## 十、域名 + HTTPS

**方案一 · 阿里云 SLB 终结 SSL（推荐）**
1. 阿里云 SSL 证书 → 申请免费 DV 证书 → 部署到 SLB；
2. SLB 监听 443 → 转发 ECS:80；监听 80 → 重定向 443；
3. 域名 A 记录指向 SLB 公网 IP。ECS 上前端 nginx 仍跑 80，SSL 全在 SLB 终结。

**方案二 · ECS 直接挂证书（无 SLB）**：证书放 `/data/mindcrew/ssl/{fullchain.pem,privkey.pem}`，前端容器映射 `443:443` 并挂载 `ssl` 目录，`nginx.conf` 加 443 server block。

## 十一、监控告警（推荐）

| 监控对象 | 工具 |
|---|---|
| ECS CPU / 内存 / 磁盘 | 阿里云云监控（免费）+ 告警短信 / 钉钉 |
| 应用健康 | 轮询 `http://<ip>:8090/actuator/health` |
| LLM token 消耗 | 后台 → 数据大屏 → 累计成本 |

## 十二、资源消耗参考（生产负载）

| 场景 | 后端 | Milvus | 建议 |
|---|---|---|---|
| 50 用户在线 + 偶尔提问 | ~5% / 1.5GB | ~10% / 2GB | 单台 4C8G 够 |
| 500 用户压测 | ~60% / 4GB | ~40% / 6GB | ECS × 2 + SLB |
| 上传 100 视频 / 小时 | FFmpeg+VL 瞬时打满 / 3GB | — | 不影响在线问答 |

## 十三、故障速查（按报错）

| 报错 | 解决 |
|---|---|
| `Communications link failure` | MySQL/RDS 不通 → 检查 `MYSQL_HOST` + 白名单 |
| `RedisConnectionFailureException` | Redis 不通 → 检查 `REDIS_HOST` / `REDIS_PASSWORD` |
| `BAILIAN_API_KEY required` | 没配大模型 Key → 填 `.env*` 的 `BAILIAN_API_KEY` |
| `OSSException: AccessDenied` | RAM 子账号没授权 → 加 `AliyunOSSFullAccess` |
| `connection refused: milvus:19530` | Milvus 没起 → `logs milvus` |
| 前端 502 | 后端还没 ready（首启 1-2 分钟）→ 稍候；或 `logs backend` 查启动报错 |
| 视频解析报「本地 MinIO 不可达」 | 生产须 `STORAGE_TYPE=oss` 并填好 `OSS_*` |

**回滚**：`IMAGE_TAG=<旧tag> ./deploy.sh update`（方案 B）；破坏性 DDL 需手动执行回滚 SQL。
**磁盘满**：`docker system prune -a --volumes --filter "until=24h"` 清缓存；清 Milvus 日志 / 已迁 OSS 的旧上传文件。

---

*本方案基于当前部署脚本与编排文件编写，具体以实际交付版本的脚本为准。*
